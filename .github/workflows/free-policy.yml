name: free-policy

# 本ワークフローは genzouw/ci-workflows の reusable workflow を呼び出す薄いスタブ。
# ロジックの実体・変更は ci-workflows 側で一元管理する (トリガー定義のみここで持つ)。
# 参照 SHA の更新は Dependabot (github-actions ecosystem) が自動で PR を出す。
# 将来 必須チェック (context: "free-policy / Free-only policy check") にする想定のため
# paths フィルタは付けない。
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: free-policy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  free-policy:
    uses: genzouw/ci-workflows/.github/workflows/free-policy.yml@609fd1bc106a59ca902ac9846d29de9b5956632d # v1.1.0
    with:
      # 導入直後は誤検知を観察するため警告のみ (job は成功する)。
      # 一定期間 誤検知が無いことを確認したら本行を削除し、既定の enforce: true に戻す。
      enforce: false
